{
  "phase": "local-discovery-qr-host-parity-interim",
  "environment": "macOS arm64; isolated source LAN Host at 192.168.31.241:28765 and freshly rebuilt Mac debug app/DMG; Flutter Android debug APK installed on Redmi 22041211AC",
  "implementation": {
    "qr_handoff": "The loopback-only Host endpoint returns only a stable host_id and private LAN URLs while LAN gameplay is enabled. Desktop encodes the first URL and host_id into dzmm-next://pair; Android accepts only a private URL plus non-empty host_id. Scanning does not create a credential; existing desktop approval remains mandatory.",
    "network_boundary": "The same mobile-handoff endpoint returns 403 from a LAN source, while the paired gameplay allowlist remains unchanged.",
    "fallback_order": "Android uses mDNS first, QR second and manual private address entry last. Saved host_id is still used for returning-player DHCP recovery."
  },
  "verification": {
    "backend": "61 pytest cases and Ruff passed. Coverage rejects LAN Host-control access and verifies QR source metadata is limited to host_id/private URL.",
    "mobile": "flutter analyze and 6 Flutter tests passed. Tests accept a private QR handoff and reject public/malformed payloads. A debug APK containing mDNS and camera scanner support built and installed successfully.",
    "desktop": "Vue production build, PyInstaller sidecar and CI=true Tauri debug app/DMG build passed. The active source Host returned the loopback handoff payload and returned 403 to a LAN-bound request."
  },
  "boundary": "QR is implemented and packaged but has not yet been scanned on the physical Redmi. This remains insufficient for Android recovery, Windows physical installer, two-router/DHCP, revoke/restart, concurrent submission, target-device long-play or maturity-score claims. The matrix remains 66.25/100."
}
