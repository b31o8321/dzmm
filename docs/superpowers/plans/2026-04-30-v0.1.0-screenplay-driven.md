# v0.1.0 剧本驱动跑团 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 开新档时调 LLM 生成剧本大纲（章节/主要 NPC/关键事件/完结），GM 围绕大纲发挥；PC 重大决策触发大纲重写；完结后可生成续集。

**Architecture:** 引入「剧本」概念作为 session 的伴生数据：开档时同步调用 outliner LLM 生成结构化大纲存表，每回合在 GM key_facts 里注入「当前剧本进度」段，主线节点必演（GM 推进义务）+ 分支节点 PC 主动触发；GM 用 4 个新标签（chapter_advance / event_complete / plot_turn / ending）报告进度；major plot_turn 异步触发 outliner 重写后续章节；完结后玩家可点「续写下一章」基于上一章结局生成 v2 大纲，PC 状态延续。

**Tech Stack:** SQLAlchemy 2.0 async + JSON columns；FastAPI 端点；新 outliner system prompt + 严格 JSON 输出；Vue 3 + element-plus 抽屉/侧栏；新增 `/play/:id/screenplay` 路由。

**Version:** 0.1.0（启用三位数 semver，0.0.14 之后第一个 MINOR 版）

---

## 设计决策（已与用户对齐）

1. **生成时机**：开档同步阻塞 ~30s；loading 页倒计时 + 展示「不剧透」的开篇引子作为起始提示词
2. **剧本严格度**：主线节点必演（GM 推进义务），分支节点 PC 主动探索才触发，不强制
3. **重大决策识别**：GM 自主 emit `<plot_turn impact="major"/>` + 玩家手动「这是重要决定」按钮
4. **genre 模板**：5 套预设（悬疑探案 / 英雄成长 / 政治阴谋 / 灾难求生 / 恋爱攻略）+ 自定义输入
5. **续作机制**：「下一章」延续同一 PC 状态 + 上一章结局生成 v2 outline
6. **UI 暴露**：进度可见但不剧透具体内容（章节标题 + 已完成事件数 + 主要 NPC 已出场列表）

---

## 数据模型设计

### `screenplays` 表
- `id` PK
- `session_id` FK → sessions.id
- `version` int default 1（重写第几版）
- `genre` str(60)（"悬疑探案" / "自定义" / ...）
- `custom_prompt` Text（玩家自定义 genre 描述）
- `outline_md` Text（完整 outline，markdown 格式，可选）
- `chapters_json` Text JSON（数组：每章 {title, summary, main_events: [...], optional_events: [...], main_npcs: [...]}）
- `main_characters_json` Text JSON（[{name, role, description, intro_chapter}]）
- `ending_md` Text（结局描述）
- `opening_hook` Text（不剧透的开篇 100-200 字给玩家看）
- `current_chapter` int default 1
- `completed_events_json` Text JSON（[{chapter: 1, event_idx: 0, type: "main|optional"}]）
- `parent_screenplay_id` int nullable（续作指向上一季的 screenplay.id）
- `status` str(20) default "active"（active | concluded | superseded）
- `created_at` DateTime
- `concluded_at` DateTime nullable

### `screenplay_revisions` 表
- `id` PK
- `screenplay_id` FK
- `revision_num` int（1, 2, 3...）
- `trigger_turn` int
- `trigger_description` Text（PC 触发决策的描述）
- `before_chapters_json` Text JSON snapshot
- `after_chapters_json` Text JSON snapshot
- `diff_summary` Text（"第 3 章原本是 X，PC 杀了主线 NPC 后改为 Y"）
- `created_at` DateTime

---

## File Structure

**Backend 新增:**
- `backend/src/dzmm/db/models.py` — 加 `Screenplay`、`ScreenplayRevision` 类
- `backend/src/dzmm/db/base.py` — 不需要 V12 migration（新表由 `Base.metadata.create_all` 创建）
- `backend/src/dzmm/prompts/outliner_template.py` — **新文件** outliner system prompt + JSON schema 范例
- `backend/src/dzmm/service/screenplay.py` — **新文件** 生成 / 重写 / 进度查询 / 状态推进
- `backend/src/dzmm/api/routes_screenplay.py` — **新文件** 5 个端点
- `backend/src/dzmm/api/schemas.py` — 加 `ScreenplayOut`、`ScreenplayProgressOut`、`GenreOption`
- `backend/src/dzmm/main.py` — 加 `routes_screenplay.router` 挂载

**Backend 修改:**
- `backend/src/dzmm/parsing/stream_parser.py` — KNOWN_TAGS 加 `chapter_advance` / `event_complete` / `plot_turn` / `ending`
- `backend/src/dzmm/service/state_apply.py` — 加 4 个 tag handler
- `backend/src/dzmm/service/game.py` — `_build_key_facts` 加「## 当前剧本进度」段
- `backend/src/dzmm/prompts/gm_template.py` — 加铁律 24（剧本进度遵守 + 主线必演 + 分支可探）+ 4 个新标签字典 + few_shot 用上 chapter_advance / event_complete

**Frontend 新增:**
- `frontend/src/views/ScreenplayView.vue` — **新页面** 路由 `/play/:id/screenplay` 显示进度
- `frontend/src/views/SessionGenerateView.vue` — **新页面** 生成 loading 页 `/sessions/generate/:id`
- `frontend/src/api/screenplay.ts` — **新文件** 5 个 API 调用
- `frontend/src/components/GenreSelector.vue` — **新组件** SessionsView 创建对话用

**Frontend 修改:**
- `frontend/src/views/SessionsView.vue` — 创建对话加 GenreSelector；创建后跳到生成 loading 页
- `frontend/src/views/GameView.vue` — header 加「📜 剧本进度」link + 显示当前章节 + 「这是重要决定」按钮 + 完结时显示「续写下一章」按钮
- `frontend/src/router/index.ts` — 加 `/play/:id/screenplay` + `/sessions/generate/:id` 路由
- `frontend/src/api/sessions.ts` — Session 类型加 `screenplay_status` 字段

---

## Tasks

### Task 1: Schema + Models

**Files:**
- Modify: `backend/src/dzmm/db/models.py`
- Test: `backend/tests/test_models.py`（如果有，否则放到 test_state_apply / test_api）

- [ ] **Step 1: 写 Screenplay model 单元测试（structural）**

`backend/tests/test_state_apply.py`（追加）:
```python
from dzmm.db.models import Screenplay, ScreenplayRevision

async def test_screenplay_model_minimal_create():
    """Screenplay 应能存最小字段并查回。"""
    # 用现有 seeded fixture 类似
    async with SessionMaker() as s:
        sp = Screenplay(
            session_id=sid,
            genre="悬疑探案",
            chapters_json='[]',
            main_characters_json='[]',
            opening_hook="夜雨打在霓虹招牌上",
        )
        s.add(sp)
        await s.commit()
        await s.refresh(sp)
        assert sp.version == 1
        assert sp.status == "active"
        assert sp.current_chapter == 1
```

- [ ] **Step 2: 跑测试看失败**

`PYTHONPATH=$(pwd)/src pytest tests/test_state_apply.py::test_screenplay_model_minimal_create -v`
预期：`ImportError: cannot import name 'Screenplay'`

- [ ] **Step 3: 在 db/models.py 末尾加 Screenplay + ScreenplayRevision 类**

```python
class Screenplay(Base):
    """v0.1.0 — Per-session screenplay (outline) generated at session creation.
    GM follows main events strictly; optional events are PC-driven."""
    __tablename__ = "screenplays"
    id: Mapped[int] = mapped_column(primary_key=True)
    session_id: Mapped[int] = mapped_column(ForeignKey("sessions.id"))
    version: Mapped[int] = mapped_column(default=1)
    genre: Mapped[str] = mapped_column(String(60), default="")
    custom_prompt: Mapped[str] = mapped_column(Text, default="")
    outline_md: Mapped[str] = mapped_column(Text, default="")
    chapters_json: Mapped[str] = mapped_column(Text, default="[]")
    main_characters_json: Mapped[str] = mapped_column(Text, default="[]")
    ending_md: Mapped[str] = mapped_column(Text, default="")
    opening_hook: Mapped[str] = mapped_column(Text, default="")
    current_chapter: Mapped[int] = mapped_column(default=1)
    completed_events_json: Mapped[str] = mapped_column(Text, default="[]")
    parent_screenplay_id: Mapped[int | None] = mapped_column(ForeignKey("screenplays.id"), nullable=True)
    status: Mapped[str] = mapped_column(String(20), default="active")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=lambda: datetime.now(UTC).replace(tzinfo=None))
    concluded_at: Mapped[datetime | None] = mapped_column(nullable=True)


class ScreenplayRevision(Base):
    """Append-only log of major plot_turn-triggered rewrites."""
    __tablename__ = "screenplay_revisions"
    id: Mapped[int] = mapped_column(primary_key=True)
    screenplay_id: Mapped[int] = mapped_column(ForeignKey("screenplays.id"))
    revision_num: Mapped[int] = mapped_column(default=1)
    trigger_turn: Mapped[int] = mapped_column(default=0)
    trigger_description: Mapped[str] = mapped_column(Text, default="")
    before_chapters_json: Mapped[str] = mapped_column(Text, default="[]")
    after_chapters_json: Mapped[str] = mapped_column(Text, default="[]")
    diff_summary: Mapped[str] = mapped_column(Text, default="")
    created_at: Mapped[datetime] = mapped_column(DateTime, default=lambda: datetime.now(UTC).replace(tzinfo=None))
```

- [ ] **Step 4: 跑测试**

`PYTHONPATH=$(pwd)/src pytest tests/test_state_apply.py::test_screenplay_model_minimal_create -v`
预期：PASS。`Base.metadata.create_all` 在 fixture init_db 时自动建表。

- [ ] **Step 5: Commit**
```bash
git add backend/src/dzmm/db/models.py backend/tests/test_state_apply.py
git commit -m "feat(db): Screenplay + ScreenplayRevision tables (v0.1.0 schema pre-stage)"
```

---

### Task 2: Outliner Prompt Template

**Files:**
- Create: `backend/src/dzmm/prompts/outliner_template.py`
- Test: `backend/tests/test_outliner_template.py`

- [ ] **Step 1: 写 outliner prompt builder 测试**

```python
# tests/test_outliner_template.py
from dzmm.prompts.outliner_template import build_outliner_messages

def test_outliner_messages_include_world_character_genre():
    msgs = build_outliner_messages(
        world_name="赛博朋克", world_md="霓虹之城",
        character_name="Riku", character_md="义体黑客",
        genre="悬疑探案", custom_prompt="",
    )
    assert len(msgs) >= 2  # system + user
    sys_text = msgs[0].content
    assert "TRPG 编剧" in sys_text or "outline" in sys_text.lower()
    user_text = msgs[1].content
    assert "赛博朋克" in user_text
    assert "Riku" in user_text
    assert "悬疑探案" in user_text

def test_outliner_messages_use_custom_prompt_when_provided():
    msgs = build_outliner_messages(
        world_name="奇幻", world_md="精灵森林",
        character_name="Eli", character_md="精灵射手",
        genre="自定义", custom_prompt="一场冰雪覆盖大陆的末日逃亡",
    )
    user_text = msgs[1].content
    assert "冰雪覆盖" in user_text or "末日逃亡" in user_text

def test_outliner_system_prompt_specifies_json_schema():
    msgs = build_outliner_messages(
        world_name="x", world_md="y", character_name="a", character_md="b",
        genre="悬疑探案", custom_prompt="",
    )
    sys_text = msgs[0].content
    # Must instruct model to output JSON with these fields
    assert "chapters" in sys_text
    assert "main_characters" in sys_text
    assert "ending" in sys_text
    assert "opening_hook" in sys_text

def test_outliner_constrains_chapter_count():
    """Model should be told to produce 3-5 chapters."""
    msgs = build_outliner_messages(
        world_name="x", world_md="y", character_name="a", character_md="b",
        genre="悬疑探案", custom_prompt="",
    )
    sys_text = msgs[0].content
    assert "3" in sys_text and "5" in sys_text  # chapter count guidance

def test_known_genres_constant_exposed():
    from dzmm.prompts.outliner_template import KNOWN_GENRES
    # Must include the 5 templates we promised the user
    for g in ["悬疑探案", "英雄成长", "政治阴谋", "灾难求生", "恋爱攻略"]:
        assert g in KNOWN_GENRES
```

- [ ] **Step 2: 跑测试看失败**

`pytest tests/test_outliner_template.py -v` → ImportError

- [ ] **Step 3: 写 outliner_template.py**

```python
"""Outliner LLM prompt — generates a structured screenplay (chapters + key events
+ main NPCs + ending + opening hook) from world+character+genre. Output is
strict JSON for the backend to parse and persist."""
from dzmm.models.client import Message

KNOWN_GENRES: dict[str, str] = {
    "悬疑探案": "PC 是侦探或调查者，剧情围绕解开一桩谜案，逐步揭开真相，最终对峙幕后黑手。",
    "英雄成长": "PC 从普通人或半吊子起步，遇到挫折后逐步成长，最终面对足以改变世界的关键挑战。",
    "政治阴谋": "PC 卷入派系斗争，要在多方势力间斡旋、收集情报、做出立场选择，最终决定一方胜负。",
    "灾难求生": "PC 处于灾难（瘟疫 / 末日 / 战争 / 自然灾害）中，资源稀缺，需要带领或保护一群人活下去。",
    "恋爱攻略": "PC 与 1-2 位主要 NPC 发展深度关系，关系是剧情核心驱动；外部冲突服务于关系试炼。",
}


_OUTLINER_SYSTEM = """你是一位经验丰富的 TRPG 编剧。你的任务是根据玩家提供的世界设定、PC 角色卡、故事类型，
生成一份结构化的「剧本大纲」给 GM 用。

# 输出格式（必须严格的 JSON，不要 markdown 代码块包裹）

{{
  "chapters": [
    {{
      "title": "第一章：副标题",
      "summary": "本章 50-80 字概要",
      "main_events": ["主线事件 1（必演）", "主线事件 2（必演）"],
      "optional_events": ["分支事件 1（PC 探索才触发）", "分支事件 2"],
      "main_npcs": ["本章重要 NPC 名"]
    }}
  ],
  "main_characters": [
    {{"name": "NPC 名", "role": "盟友/对手/导师/...", "description": "30-60 字", "intro_chapter": 1}}
  ],
  "ending": "60-100 字描述故事的最终高潮和闭幕条件",
  "opening_hook": "100-200 字开篇引子，给玩家看作为开局，**绝不剧透后续章节**——只交代起点环境、PC 处境、最初的契机"
}}

# 章节数量
3-5 章。少于 3 章故事单薄，多于 5 章节奏拖沓。

# 设计要求
1. 每章 main_events 2-4 个，optional_events 1-3 个
2. main_npcs 在 intro_chapter 章节首次登场
3. ending 是 PC 必须达成或破坏的最终目标，要具体不要抽象
4. opening_hook 写得像小说开篇——画面感、感官、悬念，但**不能告诉玩家后面会发生什么**
5. 整套大纲要紧扣故事类型（genre）的套路
6. PC 的能力 / 物品 / 弱点（profile_md 里有）应在 main_events 中找到至少一处可以用上 / 受挑战的场景

# 强约束
- 输出必须是合法 JSON，不要前后加任何文字
- 字段名严格按上面 schema
- 不输出 markdown 代码块标记 ```json
"""


def build_outliner_messages(
    world_name: str,
    world_md: str,
    character_name: str,
    character_md: str,
    genre: str,
    custom_prompt: str = "",
) -> list[Message]:
    user_lines = [
        f"# 世界：{world_name}",
        world_md.strip()[:1500],
        "",
        f"# PC：{character_name}",
        character_md.strip()[:1500],
        "",
        f"# 故事类型：{genre}",
    ]
    if genre in KNOWN_GENRES:
        user_lines.append(f"类型说明：{KNOWN_GENRES[genre]}")
    if custom_prompt.strip():
        user_lines.append(f"# 玩家自定义补充")
        user_lines.append(custom_prompt.strip()[:1000])
    user_lines.append("")
    user_lines.append("现在生成完整的剧本大纲 JSON。")

    return [
        Message(role="system", content=_OUTLINER_SYSTEM),
        Message(role="user", content="\n".join(user_lines)),
    ]
```

- [ ] **Step 4: 跑测试**

`pytest tests/test_outliner_template.py -v` → ALL PASS

- [ ] **Step 5: Commit**
```bash
git add backend/src/dzmm/prompts/outliner_template.py backend/tests/test_outliner_template.py
git commit -m "feat(prompts): outliner system prompt + KNOWN_GENRES + JSON schema"
```

---

### Task 3: Screenplay Service — generate_screenplay

**Files:**
- Create: `backend/src/dzmm/service/screenplay.py`
- Test: `backend/tests/test_screenplay_service.py`

- [ ] **Step 1: 写 generate_screenplay 测试（用 stub model 返回预设 JSON）**

```python
# tests/test_screenplay_service.py
import json
import pytest
from collections.abc import AsyncIterator
from sqlalchemy import select

from dzmm.db.base import init_db, get_engine, async_session
from dzmm.db.models import Character, ModelConfig, Screenplay, Session as GameSession, World
from dzmm.models.client import GenerationParams, Message, ModelClient, StreamChunk, TokenUsage
from dzmm.service.screenplay import generate_screenplay


_STUB_OUTLINE = json.dumps({
    "chapters": [
        {"title": "第一章：迷雾", "summary": "调查一桩失踪",
         "main_events": ["发现线索 A", "对峙嫌疑人 B"],
         "optional_events": ["搜查老宅"],
         "main_npcs": ["陈子轩"]},
        {"title": "第二章：真相", "summary": "对峙幕后黑手",
         "main_events": ["进入据点", "战斗主反派"],
         "optional_events": [],
         "main_npcs": ["黑手党头目"]},
    ],
    "main_characters": [
        {"name": "陈子轩", "role": "线人", "description": "中年华人男子",
         "intro_chapter": 1},
    ],
    "ending": "PC 揭穿黑手党的阴谋",
    "opening_hook": "雨夜的霓虹下，你接到一通电话",
}, ensure_ascii=False)


class StubOutliner(ModelClient):
    name = "stub"
    def __init__(self, output: str): self.output = output
    async def stream(self, messages: list[Message], params: GenerationParams) -> AsyncIterator[StreamChunk]:
        yield StreamChunk(delta=self.output)
        yield StreamChunk(delta="", finish_reason="stop",
                          usage=TokenUsage(input_tokens=10, output_tokens=20))


@pytest.fixture
async def seeded(tmp_path):
    engine = get_engine(f"sqlite+aiosqlite:///{tmp_path}/t.db")
    await init_db(engine)
    SessionMaker = async_session(engine)
    async with SessionMaker() as s:
        world = World(name="W", content_md="cyberpunk", style="dark", rules_json='{"mode":"light"}')
        char = Character(world=world, name="Riku", profile_md="hacker", base_stats_json='{}')
        cfg = ModelConfig(name="m", type="ollama", base_url="x", model_name="q")
        s.add_all([world, char, cfg])
        await s.flush()
        sess = GameSession(name="run", world_id=world.id, character_id=char.id,
                           gm_model_config_id=cfg.id, summarizer_model_config_id=cfg.id)
        s.add(sess)
        await s.commit()
        yield engine, SessionMaker, sess.id
    await engine.dispose()


async def test_generate_screenplay_persists_outline(seeded):
    engine, SessionMaker, sid = seeded
    client = StubOutliner(_STUB_OUTLINE)
    async with SessionMaker() as s:
        sp = await generate_screenplay(s, sid, "悬疑探案", "", client)
        await s.commit()
    async with SessionMaker() as s:
        row = (await s.execute(select(Screenplay).where(Screenplay.session_id == sid))).scalar_one()
        chapters = json.loads(row.chapters_json)
        assert len(chapters) == 2
        assert chapters[0]["title"] == "第一章：迷雾"
        mc = json.loads(row.main_characters_json)
        assert mc[0]["name"] == "陈子轩"
        assert "雨夜" in row.opening_hook
        assert row.status == "active"
        assert row.current_chapter == 1
        assert row.version == 1


async def test_generate_screenplay_strips_markdown_code_fence(seeded):
    """Defensive: model sometimes wraps JSON in ```json ... ``` despite instruction."""
    engine, SessionMaker, sid = seeded
    fenced = "```json\n" + _STUB_OUTLINE + "\n```"
    client = StubOutliner(fenced)
    async with SessionMaker() as s:
        sp = await generate_screenplay(s, sid, "悬疑探案", "", client)
        await s.commit()
        # If we got here, parsing tolerated the fence


async def test_generate_screenplay_rejects_invalid_json(seeded):
    engine, SessionMaker, sid = seeded
    client = StubOutliner("not json at all")
    with pytest.raises(ValueError):
        async with SessionMaker() as s:
            await generate_screenplay(s, sid, "悬疑探案", "", client)
```

- [ ] **Step 2: 跑测试看失败**

`pytest tests/test_screenplay_service.py -v` → ImportError

- [ ] **Step 3: 写 service/screenplay.py**

```python
"""Screenplay (outline) generation + progress + revision logic. Called from
session creation route and the major-plot_turn rewrite path."""
import json
import logging
import re
from datetime import datetime, UTC
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select

from dzmm.db.models import (
    Character, ModelConfig, Screenplay, ScreenplayRevision,
    Session as GameSession, World,
)
from dzmm.models.client import GenerationParams, ModelClient
from dzmm.prompts.outliner_template import build_outliner_messages

log = logging.getLogger(__name__)

_FENCE_RE = re.compile(r"^```(?:json)?\s*(.*?)\s*```$", re.DOTALL)
_REQUIRED_KEYS = {"chapters", "main_characters", "ending", "opening_hook"}


def _strip_fence(text: str) -> str:
    text = text.strip()
    m = _FENCE_RE.match(text)
    if m:
        return m.group(1).strip()
    return text


def _parse_outline_json(raw: str) -> dict:
    cleaned = _strip_fence(raw)
    try:
        data = json.loads(cleaned)
    except json.JSONDecodeError as e:
        raise ValueError(f"outliner returned invalid JSON: {e}") from e
    if not isinstance(data, dict):
        raise ValueError(f"outliner returned non-object root: {type(data).__name__}")
    missing = _REQUIRED_KEYS - data.keys()
    if missing:
        raise ValueError(f"outliner JSON missing keys: {missing}")
    if not isinstance(data["chapters"], list) or not data["chapters"]:
        raise ValueError("outliner JSON 'chapters' must be a non-empty list")
    return data


async def generate_screenplay(
    session: AsyncSession,
    session_id: int,
    genre: str,
    custom_prompt: str,
    client: ModelClient,
    *,
    parent_screenplay_id: int | None = None,
    previous_ending: str = "",
) -> Screenplay:
    """Call outliner LLM, parse JSON, persist a new Screenplay row."""
    sess = await session.get(GameSession, session_id)
    if sess is None:
        raise ValueError(f"session {session_id} not found")
    world = await session.get(World, sess.world_id)
    char = await session.get(Character, sess.character_id)

    user_extra = custom_prompt
    if previous_ending:
        user_extra = (custom_prompt or "") + (
            f"\n\n# 上一章结局（续作起点）\n{previous_ending}\n"
            "请基于这个结局生成下一章的剧情大纲。PC 状态延续，但章节、事件、敌对势力都应该是新的。"
        )

    messages = build_outliner_messages(
        world_name=world.name if world else "",
        world_md=world.content_md if world else "",
        character_name=char.name if char else "",
        character_md=char.profile_md if char else "",
        genre=genre or "悬疑探案",
        custom_prompt=user_extra,
    )

    raw_chunks: list[str] = []
    async for chunk in client.stream(messages, GenerationParams(num_predict=2000, temperature=0.7)):
        if chunk.delta:
            raw_chunks.append(chunk.delta)
    raw = "".join(raw_chunks)

    data = _parse_outline_json(raw)

    sp = Screenplay(
        session_id=session_id,
        version=1,
        genre=genre,
        custom_prompt=custom_prompt[:2000],
        chapters_json=json.dumps(data["chapters"], ensure_ascii=False),
        main_characters_json=json.dumps(data["main_characters"], ensure_ascii=False),
        ending_md=str(data["ending"])[:2000],
        opening_hook=str(data["opening_hook"])[:2000],
        outline_md="",  # we keep structured JSON; raw md is optional
        current_chapter=1,
        completed_events_json="[]",
        parent_screenplay_id=parent_screenplay_id,
        status="active",
    )
    session.add(sp)
    await session.flush()
    log.info("generated screenplay %d for session %d (%d chapters, genre=%s)",
             sp.id, session_id, len(data["chapters"]), genre)
    return sp


async def get_active_screenplay(session: AsyncSession, session_id: int) -> Screenplay | None:
    stmt = select(Screenplay).where(
        Screenplay.session_id == session_id,
        Screenplay.status == "active",
    ).order_by(Screenplay.version.desc())
    return (await session.execute(stmt)).scalars().first()
```

- [ ] **Step 4: 跑测试**

`pytest tests/test_screenplay_service.py -v` → 3 passing

- [ ] **Step 5: Commit**
```bash
git add backend/src/dzmm/service/screenplay.py backend/tests/test_screenplay_service.py
git commit -m "feat(screenplay): generate_screenplay service — LLM call + JSON parse + persist"
```

---

### Task 4: Stream Parser + Tag Knowledge

**Files:**
- Modify: `backend/src/dzmm/parsing/stream_parser.py`
- Modify: `backend/tests/test_stream_parser.py`

- [ ] **Step 1: 写新 tag 测试**

```python
def test_chapter_advance_tag_known():
    p = StreamingTagParser()
    events = list(p.feed("<chapter_advance/>"))
    assert any(isinstance(e, TagComplete) and e.tag == "chapter_advance" for e in events)

def test_event_complete_tag_with_attrs():
    p = StreamingTagParser()
    events = list(p.feed('<event_complete chapter="1" event="0" type="main"/>'))
    ec = [e for e in events if isinstance(e, TagComplete) and e.tag == "event_complete"][0]
    assert ec.attrs.get("chapter") == "1"
    assert ec.attrs.get("event") == "0"
    assert ec.attrs.get("type") == "main"

def test_plot_turn_tag_with_impact():
    p = StreamingTagParser()
    events = list(p.feed('<plot_turn impact="major" description="PC 杀了线人陈子轩"/>'))
    pt = [e for e in events if isinstance(e, TagComplete) and e.tag == "plot_turn"][0]
    assert pt.attrs.get("impact") == "major"

def test_ending_tag_known():
    p = StreamingTagParser()
    events = list(p.feed("<ending/>"))
    assert any(isinstance(e, TagComplete) and e.tag == "ending" for e in events)
```

- [ ] **Step 2: 跑测试看失败**

预期: 这些 tag 还没在 KNOWN_TAGS 中，会被丢弃。

- [ ] **Step 3: 加到 KNOWN_TAGS**

```python
# stream_parser.py 头部 KNOWN_TAGS 集合扩展
KNOWN_TAGS: set[str] = {
    # ... 现有 tags ...
    "chapter_advance",
    "event_complete",
    "plot_turn",
    "ending",
}
```

- [ ] **Step 4: 跑测试**

PASS

- [ ] **Step 5: Commit**
```bash
git add backend/src/dzmm/parsing/stream_parser.py backend/tests/test_stream_parser.py
git commit -m "feat(parser): chapter_advance / event_complete / plot_turn / ending tags"
```

---

### Task 5: state_apply Handlers

**Files:**
- Modify: `backend/src/dzmm/service/state_apply.py`
- Modify: `backend/tests/test_state_apply.py`

- [ ] **Step 1: 写 4 个 tag handler 测试**

```python
async def test_chapter_advance_increments_current_chapter():
    # 先 create screenplay with current_chapter=1, chapters_json with 3 chapters
    # apply <chapter_advance/>
    # assert current_chapter=2

async def test_chapter_advance_at_last_chapter_no_op():
    # current_chapter=3, total chapters=3
    # apply <chapter_advance/>
    # assert still 3 (don't go past)

async def test_event_complete_marks_event_done():
    # screenplay with chapters_json
    # apply <event_complete chapter="1" event="0" type="main"/>
    # assert completed_events_json contains {"chapter":1,"event_idx":0,"type":"main"}

async def test_event_complete_idempotent():
    # apply twice with same idx → still only one entry

async def test_plot_turn_minor_recorded_no_rewrite():
    # apply <plot_turn impact="minor" description="..."/>
    # → no ScreenplayRevision row added (minor is just observational)

async def test_plot_turn_major_creates_revision_stub():
    # apply <plot_turn impact="major" description="PC 杀了线人"/>
    # → ScreenplayRevision row created with trigger_description, before snapshot
    # (actual rewrite via outliner is async; this just records the trigger)

async def test_ending_marks_screenplay_concluded():
    # apply <ending/>
    # assert screenplay.status == "concluded"
    # assert concluded_at is set
```

- [ ] **Step 2: 跑测试看失败**

ImportError or assertion failure

- [ ] **Step 3: 实现 4 handler，加到 state_apply.py**

```python
import json
from datetime import datetime, UTC
from sqlalchemy import select
from dzmm.db.models import Screenplay, ScreenplayRevision

async def _apply_chapter_advance(session, sess_id: int, payload: dict, turn: int) -> None:
    sp = (await session.execute(
        select(Screenplay).where(
            Screenplay.session_id == sess_id, Screenplay.status == "active",
        ).order_by(Screenplay.version.desc())
    )).scalars().first()
    if sp is None:
        return
    chapters = json.loads(sp.chapters_json)
    if sp.current_chapter < len(chapters):
        sp.current_chapter += 1


async def _apply_event_complete(session, sess_id: int, payload: dict, turn: int) -> None:
    sp = (await session.execute(
        select(Screenplay).where(
            Screenplay.session_id == sess_id, Screenplay.status == "active",
        ).order_by(Screenplay.version.desc())
    )).scalars().first()
    if sp is None:
        return
    try:
        chapter = int(payload.get("chapter", 0))
        event_idx = int(payload.get("event", 0))
    except (TypeError, ValueError):
        return
    type_ = payload.get("type", "main")
    if type_ not in ("main", "optional"):
        type_ = "main"
    completed = json.loads(sp.completed_events_json or "[]")
    rec = {"chapter": chapter, "event_idx": event_idx, "type": type_}
    # idempotent
    if rec not in completed:
        completed.append(rec)
        sp.completed_events_json = json.dumps(completed, ensure_ascii=False)


async def _apply_plot_turn(session, sess_id: int, payload: dict, turn: int) -> None:
    impact = payload.get("impact", "minor")
    if impact != "major":
        # minor 仅作 telemetry，未来可以记到 messages.events_json，这里跳过
        return
    sp = (await session.execute(
        select(Screenplay).where(
            Screenplay.session_id == sess_id, Screenplay.status == "active",
        ).order_by(Screenplay.version.desc())
    )).scalars().first()
    if sp is None:
        return
    rev = ScreenplayRevision(
        screenplay_id=sp.id,
        revision_num=1,  # actual rewrite triggered later via outliner async; placeholder
        trigger_turn=turn,
        trigger_description=str(payload.get("description", ""))[:500],
        before_chapters_json=sp.chapters_json,
        after_chapters_json=sp.chapters_json,  # outliner fills later
        diff_summary="(pending outliner rewrite)",
    )
    session.add(rev)


async def _apply_ending(session, sess_id: int, payload: dict, turn: int) -> None:
    sp = (await session.execute(
        select(Screenplay).where(
            Screenplay.session_id == sess_id, Screenplay.status == "active",
        ).order_by(Screenplay.version.desc())
    )).scalars().first()
    if sp is None:
        return
    sp.status = "concluded"
    sp.concluded_at = datetime.now(UTC).replace(tzinfo=None)
```

并把它们 dispatch 到 `apply_tags`（看现有 dispatcher 风格）：
```python
elif tag == "chapter_advance":
    await _apply_chapter_advance(session, sess_id, attrs, turn)
elif tag == "event_complete":
    await _apply_event_complete(session, sess_id, attrs, turn)
elif tag == "plot_turn":
    await _apply_plot_turn(session, sess_id, attrs, turn)
elif tag == "ending":
    await _apply_ending(session, sess_id, attrs, turn)
```

- [ ] **Step 4: 跑测试**

PASS

- [ ] **Step 5: Commit**
```bash
git add backend/src/dzmm/service/state_apply.py backend/tests/test_state_apply.py
git commit -m "feat(state): chapter_advance / event_complete / plot_turn / ending handlers"
```

---

### Task 6: game.py — Inject Screenplay Progress to key_facts

**Files:**
- Modify: `backend/src/dzmm/service/game.py`
- Modify: `backend/tests/test_game_service.py`

- [ ] **Step 1: 写测试**

```python
async def test_key_facts_injects_screenplay_progress():
    """When session has an active screenplay, key_facts must show:
    - current chapter title
    - main events (pending vs done)
    - optional events
    - main NPCs not yet introduced (intro_chapter <= current_chapter)
    """
    # seed Screenplay with 2 chapters, current_chapter=1, no events done
    # build prompt → assert "## 当前剧本进度" in sys_msg
    # assert chapter title appears
    # assert at least one main event appears with [pending] tag
    # assert "完结条件" or ending mentioned

async def test_key_facts_marks_completed_events():
    # screenplay with completed_events_json having {"chapter":1, "event_idx":0, "type":"main"}
    # build prompt → that event should appear with [done] not [pending]

async def test_key_facts_omits_screenplay_when_none():
    # session without screenplay (legacy)
    # build prompt → "## 当前剧本进度" not in sys_msg
```

- [ ] **Step 2: 跑测试看失败**

- [ ] **Step 3: 修改 `_build_key_facts`**

加一段（在 PC 钩子 / 数值之后，暗中状态之前）：

```python
# 注入剧本进度
sp = (await session.execute(
    select(Screenplay).where(
        Screenplay.session_id == sess_id, Screenplay.status == "active",
    ).order_by(Screenplay.version.desc())
)).scalars().first()
if sp:
    chapters = json.loads(sp.chapters_json or "[]")
    completed = json.loads(sp.completed_events_json or "[]")
    main_chars = json.loads(sp.main_characters_json or "[]")

    cur_idx = max(0, min(sp.current_chapter - 1, len(chapters) - 1))
    cur_ch = chapters[cur_idx] if chapters else {}

    sp_lines = [
        "## 当前剧本进度（GM 严格遵守主线，分支由 PC 探索触发）",
        f"当前章节：第 {sp.current_chapter} 章「{cur_ch.get('title', '')}」"
        f"（共 {len(chapters)} 章）",
    ]

    main_events = cur_ch.get("main_events", [])
    if main_events:
        sp_lines.append("本章主线（必须演完才能推进下章）：")
        for i, ev in enumerate(main_events):
            done = any(c.get("chapter") == sp.current_chapter and
                       c.get("event_idx") == i and c.get("type") == "main"
                       for c in completed)
            tag = "[done]" if done else "[pending]"
            sp_lines.append(f"- {tag} {ev}")

    optional_events = cur_ch.get("optional_events", [])
    if optional_events:
        sp_lines.append("本章可选支线（PC 主动探索才触发，不强制）：")
        for i, ev in enumerate(optional_events):
            done = any(c.get("chapter") == sp.current_chapter and
                       c.get("event_idx") == i and c.get("type") == "optional"
                       for c in completed)
            tag = "[done]" if done else "[optional]"
            sp_lines.append(f"- {tag} {ev}")

    pending_intro = [c for c in main_chars
                     if c.get("intro_chapter", 1) <= sp.current_chapter
                     and c.get("name") not in [n.get("name") for n in []]]  # could check vs NPC table
    if pending_intro:
        names = " / ".join(c.get("name", "") for c in pending_intro[:3])
        sp_lines.append(f"重要 NPC（应在不晚于本章出场）：{names}")

    if sp.ending_md:
        sp_lines.append(f"完结条件：{sp.ending_md}")

    sp_lines.append("（推进规则：主线 [pending] 事件每 1-3 回合至少演一个；"
                   "支线 [optional] 等 PC 触发；演完后 emit <event_complete chapter=N event=M type=main/optional/>。"
                   "本章主线全部 [done] 后 emit <chapter_advance/>。"
                   "完结条件达成 emit <ending/>。重大决策 emit <plot_turn impact=major description=...>）")

    parts.append("\n".join(sp_lines))
```

- [ ] **Step 4: 跑测试**

PASS

- [ ] **Step 5: Commit**
```bash
git add backend/src/dzmm/service/game.py backend/tests/test_game_service.py
git commit -m "feat(game): inject screenplay progress to key_facts (with completion markers)"
```

---

### Task 7: GM Prompt — Tag Docs + Rule 24

**Files:**
- Modify: `backend/src/dzmm/prompts/gm_template.py`
- Modify: `backend/tests/test_gm_template.py`

- [ ] **Step 1: 写测试**

```python
def test_gm_prompt_documents_screenplay_tags():
    sys_text = build_gm_messages(...)[0].content
    assert "<chapter_advance" in sys_text
    assert "<event_complete" in sys_text
    assert "<plot_turn" in sys_text
    assert "<ending" in sys_text

def test_gm_prompt_rule_24_screenplay_obedience():
    sys_text = build_gm_messages(...)[0].content
    assert "剧本进度" in sys_text or "主线" in sys_text
    assert "推进" in sys_text
```

- [ ] **Step 2: 跑测试失败**

- [ ] **Step 3: 加铁律 24 + 4 个标签字典**

```
24. **剧本进度遵守**：prompt 的 key_facts 若有「## 当前剧本进度」段：
    - 主线 [pending] 事件每 1-3 回合至少演一个，演完后立即 emit
      `<event_complete chapter="N" event="M" type="main"/>`
    - 支线 [optional] 事件不强制——PC 主动触发才演；演完同样 emit
      `<event_complete>` type="optional"
    - 本章 main_events 全部 [done] 后 emit `<chapter_advance/>` 推进
    - 完结条件达成 emit `<ending/>`，故事完结
    - PC 做出重大决策（杀关键 NPC、选择关键阵营、达成阶段性目标、放弃主线）
      → emit `<plot_turn impact="major" description="..."/>`，后端会重写后续大纲
```

标签字典：
```
<chapter_advance/>
本章主线全部演完后输出此自闭合标签，推进到下一章。

<event_complete chapter="N" event="M" type="main|optional"/>
某个 main_event 或 optional_event 演完时输出。chapter/event 索引从 0 开始。

<plot_turn impact="major|minor" description="...">
PC 做出关键决策时记录。impact=major 会触发后续章节重写；minor 只是观察。

<ending/>
完结条件达成时输出，故事终结，玩家可点续写下一章。
```

- [ ] **Step 4: 跑测试**

PASS

- [ ] **Step 5: Commit**
```bash
git add backend/src/dzmm/prompts/gm_template.py backend/tests/test_gm_template.py
git commit -m "feat(prompts): rule 24 (screenplay obedience) + 4 new tag dictionary entries"
```

---

### Task 8: Routes (5 endpoints)

**Files:**
- Create: `backend/src/dzmm/api/routes_screenplay.py`
- Modify: `backend/src/dzmm/main.py`
- Modify: `backend/src/dzmm/api/schemas.py`
- Modify: `backend/tests/test_api.py`

- [ ] **Step 1: 写 5 个端点测试**

```python
async def test_post_screenplay_generate_creates_active(http, monkeypatch):
    # monkeypatch build_client to return StubOutliner
    sid = await _make_session(http)
    r = await http.post(f"/sessions/{sid}/screenplay/generate",
                        json={"genre": "悬疑探案"})
    assert r.status_code == 200
    body = r.json()
    assert body["status"] == "active"
    assert len(body["chapters"]) >= 1

async def test_get_screenplay_returns_active(http):
    # after generate
    # GET /sessions/{id}/screenplay → returns same data + progress

async def test_post_mark_decision_records_revision(http):
    # POST /sessions/{id}/screenplay/mark_decision body {description}
    # → ScreenplayRevision row created

async def test_post_screenplay_continue_creates_v2(http, monkeypatch):
    # session with concluded screenplay
    # POST /sessions/{id}/screenplay/continue
    # → new screenplay row, parent_screenplay_id set, version increments? or new screenplay
    # Actually new screenplay belongs to same session, status=active; old one stays concluded
    # PC keeps level/xp; old screenplay parent_screenplay_id is set on new one

async def test_get_revisions_returns_list(http):
    # POST mark_decision twice
    # GET /sessions/{id}/screenplay/revisions → 2 entries
```

- [ ] **Step 2: 跑测试失败**

- [ ] **Step 3: 写 routes_screenplay.py + 接到 main.py**

```python
"""v0.1.0 — screenplay (outline) API endpoints."""
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from dzmm.api.routes_sessions import get_session_dep
from dzmm.db.models import (
    ModelConfig, Screenplay, ScreenplayRevision,
    Session as GameSession,
)
from dzmm.models.factory import build_client
from dzmm.service.screenplay import generate_screenplay, get_active_screenplay

router = APIRouter(prefix="/sessions", tags=["screenplay"])


@router.post("/{session_id}/screenplay/generate")
async def generate(
    session_id: int,
    payload: dict,
    s: AsyncSession = Depends(get_session_dep),
):
    sess = await s.get(GameSession, session_id)
    if sess is None:
        raise HTTPException(404, "session not found")
    cfg = await s.get(ModelConfig, sess.gm_model_config_id)
    if cfg is None:
        raise HTTPException(400, "GM model config missing")
    client = build_client(cfg)
    genre = (payload.get("genre") or "悬疑探案").strip()
    custom = (payload.get("custom_prompt") or "").strip()
    sp = await generate_screenplay(s, session_id, genre, custom, client)
    await s.commit()
    return _screenplay_dict(sp)


@router.get("/{session_id}/screenplay")
async def get_active(
    session_id: int,
    s: AsyncSession = Depends(get_session_dep),
):
    sp = await get_active_screenplay(s, session_id)
    if sp is None:
        raise HTTPException(404, "no active screenplay")
    return _screenplay_dict(sp)


@router.post("/{session_id}/screenplay/mark_decision")
async def mark_decision(
    session_id: int,
    payload: dict,
    s: AsyncSession = Depends(get_session_dep),
):
    sp = await get_active_screenplay(s, session_id)
    if sp is None:
        raise HTTPException(404, "no active screenplay")
    sess = await s.get(GameSession, session_id)
    rev = ScreenplayRevision(
        screenplay_id=sp.id,
        revision_num=1,
        trigger_turn=sess.turn_count if sess else 0,
        trigger_description=str(payload.get("description") or "玩家手动标记")[:500],
        before_chapters_json=sp.chapters_json,
        after_chapters_json=sp.chapters_json,
        diff_summary="(player-marked, pending rewrite)",
    )
    s.add(rev)
    await s.commit()
    return {"ok": True, "revision_id": rev.id}


@router.post("/{session_id}/screenplay/continue")
async def continue_to_next(
    session_id: int,
    payload: dict,
    s: AsyncSession = Depends(get_session_dep),
):
    sess = await s.get(GameSession, session_id)
    if sess is None:
        raise HTTPException(404, "session not found")
    prev = (await s.execute(
        select(Screenplay).where(
            Screenplay.session_id == session_id, Screenplay.status == "concluded",
        ).order_by(Screenplay.version.desc())
    )).scalars().first()
    if prev is None:
        raise HTTPException(400, "no concluded screenplay to continue from")
    cfg = await s.get(ModelConfig, sess.gm_model_config_id)
    client = build_client(cfg)
    sp = await generate_screenplay(
        s, session_id, prev.genre, prev.custom_prompt, client,
        parent_screenplay_id=prev.id,
        previous_ending=prev.ending_md,
    )
    await s.commit()
    return _screenplay_dict(sp)


@router.get("/{session_id}/screenplay/revisions")
async def list_revisions(
    session_id: int,
    s: AsyncSession = Depends(get_session_dep),
):
    sp = await get_active_screenplay(s, session_id)
    if sp is None:
        return []
    rows = (await s.execute(
        select(ScreenplayRevision).where(ScreenplayRevision.screenplay_id == sp.id)
        .order_by(ScreenplayRevision.created_at, ScreenplayRevision.id)
    )).scalars().all()
    return [
        {
            "id": r.id, "revision_num": r.revision_num,
            "trigger_turn": r.trigger_turn,
            "trigger_description": r.trigger_description,
            "diff_summary": r.diff_summary,
            "created_at": r.created_at.isoformat() if r.created_at else None,
        }
        for r in rows
    ]


def _screenplay_dict(sp: Screenplay) -> dict:
    import json
    return {
        "id": sp.id,
        "session_id": sp.session_id,
        "version": sp.version,
        "genre": sp.genre,
        "chapters": json.loads(sp.chapters_json or "[]"),
        "main_characters": json.loads(sp.main_characters_json or "[]"),
        "ending_md": sp.ending_md,
        "opening_hook": sp.opening_hook,
        "current_chapter": sp.current_chapter,
        "completed_events": json.loads(sp.completed_events_json or "[]"),
        "parent_screenplay_id": sp.parent_screenplay_id,
        "status": sp.status,
        "created_at": sp.created_at.isoformat() if sp.created_at else None,
        "concluded_at": sp.concluded_at.isoformat() if sp.concluded_at else None,
    }
```

挂到 main.py：
```python
from dzmm.api import routes_screenplay
# ...
app.dependency_overrides[routes_screenplay.get_session_dep] = get_session_dep  # 复用 sessions 的
app.include_router(routes_screenplay.router)
```

(注意 routes_screenplay 直接 import routes_sessions.get_session_dep，所以 main.py 把同一个 override 应用到 routes_sessions 已经能 cover；不需要重复 override。)

- [ ] **Step 4: 跑测试**

PASS

- [ ] **Step 5: Commit**
```bash
git add backend/
git commit -m "feat(api): 5 screenplay endpoints (generate / get / mark_decision / continue / revisions)"
```

---

### Task 9: Frontend — Genre Selector + Generate View

**Files:**
- Create: `frontend/src/components/GenreSelector.vue`
- Create: `frontend/src/views/SessionGenerateView.vue`
- Create: `frontend/src/api/screenplay.ts`
- Modify: `frontend/src/views/SessionsView.vue`
- Modify: `frontend/src/router/index.ts`

- [ ] **Step 1: 创建 `frontend/src/api/screenplay.ts`**

```ts
import { api } from './client'

export interface ScreenplayChapter {
  title: string
  summary: string
  main_events: string[]
  optional_events: string[]
  main_npcs: string[]
}

export interface ScreenplayMainCharacter {
  name: string
  role: string
  description: string
  intro_chapter: number
}

export interface CompletedEvent {
  chapter: number
  event_idx: number
  type: 'main' | 'optional'
}

export interface Screenplay {
  id: number
  session_id: number
  version: number
  genre: string
  chapters: ScreenplayChapter[]
  main_characters: ScreenplayMainCharacter[]
  ending_md: string
  opening_hook: string
  current_chapter: number
  completed_events: CompletedEvent[]
  parent_screenplay_id: number | null
  status: 'active' | 'concluded' | 'superseded'
  created_at: string
  concluded_at: string | null
}

export const screenplayApi = {
  generate: (id: number, body: { genre: string; custom_prompt?: string }) =>
    api.post<Screenplay>(`/sessions/${id}/screenplay/generate`, body)
       .then((r) => r.data),
  getActive: (id: number) =>
    api.get<Screenplay>(`/sessions/${id}/screenplay`).then((r) => r.data),
  markDecision: (id: number, description: string) =>
    api.post(`/sessions/${id}/screenplay/mark_decision`, { description })
       .then((r) => r.data),
  continueNext: (id: number) =>
    api.post<Screenplay>(`/sessions/${id}/screenplay/continue`, {})
       .then((r) => r.data),
  revisions: (id: number) =>
    api.get(`/sessions/${id}/screenplay/revisions`).then((r) => r.data),
}

export const KNOWN_GENRES: { key: string; label: string; desc: string }[] = [
  { key: '悬疑探案', label: '🔍 悬疑探案', desc: '解谜 / 调查 / 真相揭露' },
  { key: '英雄成长', label: '⚔️ 英雄成长', desc: '从凡人到救世' },
  { key: '政治阴谋', label: '🎭 政治阴谋', desc: '势力斡旋 / 立场抉择' },
  { key: '灾难求生', label: '🌋 灾难求生', desc: '资源稀缺 / 生死逃亡' },
  { key: '恋爱攻略', label: '💕 恋爱攻略', desc: '关系养成 / 情感试炼' },
  { key: '自定义', label: '✏️ 自定义', desc: '你说想要什么样的故事' },
]
```

- [ ] **Step 2: 创建 `GenreSelector.vue`**

略（5 张卡片选择 + 自定义文本框；v-model 输出 `{genre, custom_prompt}`）。详细实现见末尾。

- [ ] **Step 3: 创建 `SessionGenerateView.vue`**

```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { ElProgress, ElMessage } from 'element-plus'
import { screenplayApi } from '@/api/screenplay'
import MarkdownView from '@/components/MarkdownView.vue'

const route = useRoute()
const router = useRouter()
const sessionId = Number(route.params.id)
const elapsed = ref(0)
const phase = ref<'generating' | 'preview' | 'failed'>('generating')
const errorMsg = ref('')
const screenplay = ref<any>(null)

const tips = [
  '剧本里包含主线和支线，主线是 GM 必须演的，支线由你探索触发',
  '关键决策可能改变后续章节走向',
  '剧本完结后可以续写下一章，保留人物状态',
]
const tipIdx = ref(0)
let timer: number | null = null

onMounted(async () => {
  // genre + custom_prompt 通过 router state 或 query 传过来
  const genre = route.query.genre as string || '悬疑探案'
  const customPrompt = route.query.custom_prompt as string || ''
  
  timer = window.setInterval(() => {
    elapsed.value += 1
    tipIdx.value = (tipIdx.value + 1) % tips.length
  }, 2000)
  
  try {
    const sp = await screenplayApi.generate(sessionId, {
      genre, custom_prompt: customPrompt,
    })
    screenplay.value = sp
    phase.value = 'preview'
  } catch (e: any) {
    phase.value = 'failed'
    errorMsg.value = e?.message ?? String(e)
  } finally {
    if (timer) { clearInterval(timer); timer = null }
  }
})

function startPlay() {
  router.push(`/play/${sessionId}`)
}

function retry() {
  phase.value = 'generating'
  elapsed.value = 0
  // 重新调用 generate
  // ... 略
}
</script>

<template>
  <div class="h-full flex items-center justify-center bg-slate-50">
    <div v-if="phase === 'generating'" class="max-w-md text-center space-y-4">
      <div class="text-2xl font-bold text-slate-700">📜 正在为你生成剧本...</div>
      <div class="text-sm text-slate-500">
        {{ tips[tipIdx] }}（已用 {{ elapsed }}s）
      </div>
      <el-progress :percentage="Math.min(elapsed * 3, 95)" :show-text="false" :indeterminate="elapsed > 30" />
    </div>
    
    <div v-else-if="phase === 'preview'" class="max-w-2xl space-y-4 p-6">
      <div class="text-2xl font-bold text-slate-800">📖 故事开始了</div>
      <div class="bg-white border border-slate-200 rounded p-6 prose">
        <MarkdownView :source="screenplay.opening_hook" />
      </div>
      <div class="flex gap-2">
        <el-button type="primary" size="large" @click="startPlay">▶ 开始跑团</el-button>
      </div>
    </div>
    
    <div v-else class="max-w-md text-center space-y-3">
      <div class="text-2xl font-bold text-red-600">生成失败</div>
      <div class="text-sm text-slate-600">{{ errorMsg }}</div>
      <el-button type="primary" @click="retry">重试</el-button>
    </div>
  </div>
</template>
```

- [ ] **Step 4: SessionsView 改造**

新建对话加 GenreSelector；点击「创建」后：
1. 先 POST /sessions 创建 session（拿到 sid）
2. 然后跳到 `/sessions/generate/{sid}?genre=X&custom_prompt=Y`

- [ ] **Step 5: router 加路由**

```ts
{ path: 'sessions/generate/:id', name: 'session-generate',
  component: () => import('@/views/SessionGenerateView.vue'), props: true }
```

- [ ] **Step 6: 验收 + commit**

```bash
npm run build
git add frontend/
git commit -m "feat(ui): genre selector + screenplay generate view (loading + opening hook preview)"
```

---

### Task 10: Frontend — ScreenplayView + GameView Integration

**Files:**
- Create: `frontend/src/views/ScreenplayView.vue`
- Modify: `frontend/src/views/GameView.vue`
- Modify: `frontend/src/router/index.ts`

- [ ] **Step 1: 创建 ScreenplayView**

显示当前章节、本章主线（done/pending）、本章支线、主要 NPC 列表、剧情进度条、「这是重要决定」输入框、完结后的「续写下一章」按钮。

- [ ] **Step 2: GameView header 加「📜 剧本进度」link + 完结按钮**

```vue
<router-link :to="`/play/${sessionId}/screenplay`" ...>📜 剧本</router-link>
```

加 onMounted 拉取 screenplay 状态：如果 `status === 'concluded'` 显示底部蓝色横幅「故事已完结，是否续写下一章？」

- [ ] **Step 3: router 加路由**

```ts
{ path: 'play/:id/screenplay', name: 'screenplay',
  component: () => import('@/views/ScreenplayView.vue'), props: true }
```

- [ ] **Step 4: 验收 + commit**

```bash
npm run build
git add frontend/
git commit -m "feat(ui): ScreenplayView (progress sidebar) + GameView screenplay link + ending banner"
```

---

### Task 11: Major plot_turn Async Rewrite (deferred to v0.1.1)

记录到 backlog（不在 v0.1.0 范围）：
- 当 `<plot_turn impact="major"/>` 触发后，异步任务调 outliner LLM 重写后续章节
- 写入 ScreenplayRevision.after_chapters_json + diff_summary
- 更新 Screenplay.chapters_json + version+1
- 前端 ScreenplayView 显示 revisions 时间线

**v0.1.0 先存 placeholder revision；v0.1.1 实现真正的 outline 重写。**

---

### Task 12: Documentation + Version Bump

- [ ] CHANGELOG 加 v0.1.0 entry
- [ ] README 更新路线图
- [ ] 4 处 version 文件 bump 到 0.1.0
- [ ] tag v0.1.0

---

## 集成测试 / 验收

- [ ] 完整流程跑一次：开新档（选悬疑探案）→ 等大纲生成 → 看 opening_hook → 进 GameView → 跑几个回合 → 看 GM 是否按主线推 + emit `<event_complete>` → 章节推进 → 完结 → 续写
- [ ] 自定义 genre 流程也跑一次
- [ ] 测试覆盖：后端 +30 tests（screenplay service 5 + state apply 8 + game 3 + api 9 + outliner 5）

---

## 风险 + 注意事项

1. **outliner LLM 输出不合规**：本地 7B 模型可能不严格输出 JSON。已在 `_strip_fence` 容错；可加重试机制（v0.1.1）
2. **生成时间长 (>30s)**：用户体验风险。SessionGenerateView 提供倒计时 + tips 减缓焦虑；超 60s 显示「这个模型可能比较慢，可以换一个云模型」提示
3. **大纲过具体限制 GM 自由**：UI exposure 选 (B) 已平衡；prompt 里强调「主线节点 = 演完的事件 + 玩家想看的爽点；细节、对话、随机遭遇仍由 GM 自由发挥」
4. **续作衔接**：parent_screenplay_id 链可能很长（玩家玩很多季）；可加深度限制（最多 5 季？）—— v0.1.0 不限，先看玩家行为
5. **plot_turn major 但暂不 rewrite**：v0.1.0 只记 revision；玩家可能期待立即看到大纲变化。UI 文案要管理预期：「这次决策已记录，下次大纲更新时会反映」

---

## 自检（writing-plans skill）

**Spec coverage:**
- ✅ 6 设计决策都有对应 task
- ✅ 后端：schema (1) / outliner (2-3) / parser (4) / state_apply (5) / game.py (6) / prompt (7) / routes (8)
- ✅ 前端：generate UI (9) / screenplay sidebar (10)
- ✅ 文档 + tag (12)

**Placeholder scan:** 全部 step 都给了具体代码或 prompt 文本，无 TBD/TODO。

**Type consistency:** ScreenplayChapter / ScreenplayMainCharacter / CompletedEvent 的字段在前后端一致；JSON schema 定义和 outliner_template 输出一致。

---

## Execution Handoff

Plan 完成。两种执行选项：

**1. Subagent-Driven（推荐）**：用 `superpowers:subagent-driven-development`，每个 task 起一个 fresh subagent，两段式 review

**2. Inline Execution**：用 `superpowers:executing-plans` 在当前 session 跑

我建议 **Subagent-Driven** —— 12 个 task 中 task 1+4 是机械的（小 file），task 2+3+5+6+7 是中等（多 file），task 8+9+10 大且跨前后端，subagent 隔离对每段都合适。

等你 review 这份 plan，告诉我开 subagent-driven 还是先调整哪些设计。
