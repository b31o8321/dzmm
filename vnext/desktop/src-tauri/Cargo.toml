[package]
name = "dzmm-next-desktop"
version = "0.1.0"
description = "DZMM macOS host"
edition = "2021"
rust-version = "1.77"

[lib]
name = "dzmm_next_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.5.6", features = [] }

[dependencies]
tauri = { version = "2.10.3", features = [] }
